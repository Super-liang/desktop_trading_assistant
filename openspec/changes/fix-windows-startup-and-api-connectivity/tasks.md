## 1. 回归测试

- [x] 1.1 增加 Windows Tauri Origin 的 CORS 允许与非允许来源拒绝测试
- [x] 1.2 增加认证状态、托盘和老板键控制 ticker 可见性的原生逻辑测试

## 2. 服务端连接修复

- [x] 2.1 将 `http://tauri.localhost` 加入服务端最小 CORS 允许列表

## 3. 桌面窗口生命周期修复

- [x] 3.1 禁止窗口状态插件恢复历史可见性，同时保留尺寸和位置状态
- [x] 3.2 增加原生认证状态命令并统一保护 ticker 的显示入口
- [x] 3.3 在前端会话变化时同步原生认证状态，未认证时隐藏 ticker

## 4. 验证

- [x] 4.1 运行服务端定向测试、桌面端测试、TypeScript 构建与 Rust 测试
- [x] 4.2 构建云 API 前端产物并验证不包含 localhost API 回退
- [x] 4.3 完成 OpenSpec 严格校验并记录本地验证结果与待 Windows 实机验证项
