# 验证报告

验证日期：2026-08-09

## 自动化验证

- Java 全量测试：`./mvnw -q test`，100 项执行，0 失败、0 错误、4 项跳过。
- Java 定向复验：`PortfolioControllerTest,PortfolioExceptionHandlerTest`，通过。
- 前端全量测试：18 个测试文件、78 项测试全部通过。
- 重复持仓与账户中心定向测试：2 个测试文件、22 项测试全部通过。
- Web 生产构建：TypeScript 编译和 Vite 构建成功。
- `git diff --check`：通过，无空白错误。

## 本地真实链路验证

在用户可见 Terminal 中重启 Spring API，并确认 `/actuator/health` 返回 `UP`。使用一次性本地账号经真实 HTTP 接口完成：

1. 新建 100 股、成本 10 的 A 股持仓；
2. 再次创建同一持仓，返回 HTTP 409、`POSITION_ALREADY_EXISTS` 和当前用户已有持仓 ID；
3. 追加 100 股、成本 20，返回总数量 200、加权成本 15；
4. 注销一次性账号，返回 204，测试数据已清理。

## 覆盖边界

- 已覆盖重复确认、取消不写、普通错误、加权成本、零持仓首次追加、越权 ID、账户中心普通/管理员入口。
- 同用户创建通过数据库用户行锁串行化；本轮未进行多进程压力测试，数据库唯一约束仍保留为最终完整性保护。
- 前端测试存在既有 TanStack Query `queryFn` 警告，但没有未处理异常且全部断言通过；不属于本次功能阻断。

## 线上交付验证

- 代码提交 `509f3ca` 已推送至 GitHub `main`。
- 发布前备份 `20260809T020900Z` 已完成格式、校验和及完整解压校验，并通过隔离恢复演练。
- 后端、AKShare 网关与网页版已原子部署至 `v0.1.6-web-20260809T020709Z`，生产 Flyway 为 V7。
- 云端完整验收通过；额外使用一次性线上账号验证重复创建返回 409、100@10 加仓 100@20 后得到 200@15，注销返回 204，测试数据已清理。
- 本次遵循用户此前要求，未构建或发布桌面客户端安装包。
