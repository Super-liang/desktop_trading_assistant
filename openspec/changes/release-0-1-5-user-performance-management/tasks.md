## 1. 数据模型与收益口径

- [x] 1.1 编写 Flyway 迁移，新增 `user_operation_audits` 与 `user_performance_daily`、约束和按用户/时间查询索引
- [x] 1.2 为收益计算器先补充日收益、零基数、部分行情、本年连乘、跨年和不足 30 天场景的失败测试
- [x] 1.3 实现收益计算器、收益 DTO 与 `COMPLETE`/`PARTIAL`/`UNAVAILABLE`/`ACCUMULATING` 数据状态，使新增测试通过
- [x] 1.4 实现使用 A 股交易日历、Asia/Shanghai 时区和统一结算源的幂等日终聚合任务，并补充重复执行、非交易日和行情未就绪测试
- [x] 1.5 新增 `GET /api/v1/me/performance`，验证本人权限、字段口径、空持仓与参考收益标识

## 2. 用户操作审计与管理员详情

- [x] 2.1 编写持仓新增、修改、删除以及密码修改审计的失败测试，断言不持久化数量、成本、密码、令牌或请求正文
- [x] 2.2 实现普通用户不可变操作审计服务，并接入持仓新增、修改、删除流程
- [x] 2.3 扩展管理员用户列表展示所需 DTO，统一最后一次密码登录时间及“尚未登录”语义
- [x] 2.4 新增管理员按用户 overview 与 holdings 接口，并用序列化测试证明响应不包含数量、成本、总市值和单只收益金额
- [x] 2.5 新增管理员按用户、动作和时间范围分页查询操作审计接口，验证用户隔离、排序、分页和非管理员拒绝访问

## 3. 修改密码

- [x] 3.1 先补充当前密码错误、弱密码、新旧相同、确认不一致、成功改密和旧刷新令牌失效的接口测试
- [x] 3.2 实现 `POST /api/v1/me/change-password`、密码哈希更新、全部刷新令牌撤销和安全审计
- [x] 3.3 实现修改密码对话框、客户端校验、错误反馈以及成功后清理本地会话并返回登录页

## 4. 单股永久缓存

- [x] 4.1 为各来源缓存隔离、无 TTL、成功覆盖、失败回退、进程重启后读取和首次无数据补充 Redis/网关测试
- [x] 4.2 实现按来源与证券 ID 保存行情 JSON 和最后成功时间的 Redis 单股缓存，保留内存缓存仅用于请求合并
- [x] 4.3 调整单股接口失败降级逻辑，缓存命中时返回最后行情与 `stale=true`，禁止在刷新间隔内清空证券行
- [x] 4.4 验证东财、雪球等现有单股来源不会跨来源冒用数据，并检查最后刷新时间展示

## 5. 桌面端页面重构

- [x] 5.1 为首页不展示持仓、金额/比率切换、我的持仓导航和新增成功跳转编写组件测试
- [x] 5.2 将共享行情查询、透明小窗广播和全局动作提取到应用壳，以单一视图状态管理首页、持仓、行情设置和管理页面
- [x] 5.3 实现仅展示行情状态与个人收益摘要的首页，以及金额/比率分段切换和参考收益说明
- [x] 5.4 实现“我的持仓”独立页面，复用现有持仓表和编辑/删除功能，并保留新增持仓入口
- [x] 5.5 调整新增持仓成功流程，刷新共享查询并跳转到我的持仓；失败时保留输入和当前页面

## 6. 管理端界面

- [x] 6.1 为最后登录空状态、用户详情隐私字段、按用户审计筛选和权限错误编写管理端组件/API 测试
- [x] 6.2 优化用户列表搜索与最后登录展示，增加进入指定用户详情的入口
- [x] 6.3 实现管理员用户详情的持仓证券清单、聚合收益卡、数据状态和分页审计列表
- [x] 6.4 检查管理端所有网络请求均以选中 userId 为边界，且前端类型中不存在管理端数量/成本字段

## 7. 手机浏览器适配

- [x] 7.1 实现不超过 900px 时的移动导航，确保首页、我的持仓、行情设置、修改密码和退出登录全部可达
- [x] 7.2 修复根节点和工作区滚动策略，将收益、行情灯、持仓和管理表格在窄屏降级为响应式卡片/列表
- [x] 7.3 调整新增/编辑持仓与修改密码对话框的宽高、内部滚动、单列字段、44px 触控目标及 safe-area
- [x] 7.4 使用 375×667 与 390×844 浏览器视口验证无页面级横向滚动、无不可达按钮和无被裁切表单

## 8. 图标与发布资源

- [x] 8.1 使用 `assets/branding/yinxian-app-icon-v1.png` 重新生成 Tauri `.icns`、`.ico` 和多尺寸 PNG
- [x] 8.2 生成并接入 Web favicon，增加源图与构建资源存在性冒烟检查
- [ ] 8.3 在 macOS Dock/Finder、Windows 可执行文件/快捷方式及浏览器标签页检查新图标的清晰度和一致性

## 9. 验证与质量门禁

- [x] 9.1 执行后端单元/集成测试、AKShare 网关测试、前端单元测试、类型检查和生产构建，并保存真实输出
- [x] 9.2 使用本地 PostgreSQL 与 Redis 启动完整服务，验证注册/登录、改密、持仓审计、收益、管理员按用户查询和单股失败回退
- [ ] 9.3 验证页面切换和后台运行期间行情轮询及透明小窗同步未回归，并检查 macOS 能耗与卡顿现场
- [ ] 9.4 独立执行 verification pass，对照每条 OpenSpec 场景形成验证报告，不与代码审查合并
- [ ] 9.5 独立执行 code-review pass，修复阻断问题后重新运行受影响验证
- [ ] 9.6 经用户本地验收确认后再提交、推送、部署云服务器并构建/发布 macOS 与 Windows 0.1.5 安装包
