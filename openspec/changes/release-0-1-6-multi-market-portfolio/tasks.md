## 1. 契约与依赖基线

- [x] 1.1 固化 0.1.6 市场/交易所/币种/资产类型枚举及七个规范首页指数标识：`SSE_INDEX:000001`、`SZSE_INDEX:399001`、`SZSE_INDEX:399006`、`BSE_INDEX:899050`、`SSE_INDEX:000680`、`SSE_INDEX:000688`、`CSI_INDEX:000300`
- [x] 1.2 为现有 AKShare 固定版本编写 A/H/美股票、基金目录/净值、沪深京指数的列名与代码契约失败测试
- [x] 1.3 在隔离环境评估并固定通过契约测试的 AKShare 版本，加入 `exchange_calendars` 固定依赖并记录许可证与数据延迟说明
- [x] 1.4 为 `XSHG`、`XHKG`、`XNYS` 会话、午休、节假日、DST 和提前收市编写可控日期测试样例

## 2. 数据库迁移与领域模型

- [x] 2.1 编写 Flyway 迁移测试，覆盖市场会话、同步运行、证券主数据扩展、持仓市场/币种/建仓日期和每日基线表
- [x] 2.2 实现只增不删的 V6+ 迁移，先回填既有 `SSE/SZSE/BSE` 数据再添加非空约束和索引
- [x] 2.3 为无法识别的旧证券标识补充迁移失败测试，确保不静默推断错误市场
- [x] 2.4 实现 Java 市场、交易所、币种、资产类型和值对象，并让旧 A 股 `InstrumentId` 保持兼容
- [x] 2.5 扩展证券目录和持仓实体，保存 `market/exchange/currency/providerSymbol/openedOn` 并增加参数化查询索引

## 3. 三地交易日历与市场时钟

- [x] 3.1 为 AKShare 网关新增日历 Provider 和规范化 DTO，生成过去 30 天、未来 400 天的 XSHG/XHKG/XNYS 会话
- [x] 3.2 实现 A 股 AKShare 交易日期交叉检查、人工修正优先级和日历覆盖/来源健康状态
- [x] 3.3 实现 Spring 日历同步客户端、`market_sessions` 幂等写入和每日 02:30 滚动同步调度
- [x] 3.4 实现统一 `MarketClock`，返回 `PRE_OPEN/OPEN/BREAK/CLOSED/HOLIDAY/UNKNOWN` 和下一开收盘时间
- [x] 3.5 用可控时钟验证 A/H 午休、美股 DST、提前收市、节假日和覆盖缺失时停止主动刷新
- [x] 3.6 新增 `GET /api/v1/markets/status` 及权限、序列化和日历降级测试

## 4. 跨市场证券目录

- [x] 4.1 重构 AKShare 目录端点为按市场加载，分别规范化 A 股、港股、美股和仅含单位净值口径的开放式基金目录，排除货币基金万份收益及封闭式基金
- [x] 4.2 实现美股东财原始代码到 NASDAQ/NYSE/AMEX 的受测映射，未知映射拒绝入库并记录脱敏计数
- [x] 4.3 实现按市场批量 upsert、下架标记、来源更新时间和旧目录保留策略
- [x] 4.4 实现 A/H/美在各自开盘前一小时的幂等目录任务、开盘前十分钟重试和有界并发
- [x] 4.5 实现北京时间 07:00 基金目录同步，并验证重复执行和上游失败不清空目录
- [x] 4.6 将证券搜索 API 改为市场隔离，补充跨市场混入、币种/交易所返回和旧 A 股客户端兼容测试

## 5. A/H/美股票与基金行情路由

- [x] 5.1 扩展 AKShare 来源能力矩阵和健康注册表，按 `market + capability + source` 暴露可用来源
- [x] 5.2 实现沪深京 A 股全市场和指数快照适配，并行拉取所需东财指数分组并按规范标识筛选，覆盖 BSE 证券及七个目标指数逐项缺失场景
- [x] 5.3 实现港股东方财富全市场快照，规范 HKD、今开/昨收和约 15 分钟延迟元数据
- [x] 5.4 实现美股东方财富快照和新浪延迟降级来源，规范 USD、交易所和常规交易时段元数据
- [x] 5.5 实现基金最新/前一单位净值、净值日期和持仓基金历史补查，确保同一净值日期幂等
- [x] 5.6 将 Redis 行情 key 重构为市场/能力/来源隔离且无主动 TTL，验证重启读取、来源失败和跨市场不污染
- [x] 5.7 将全市场调度改为 `MarketClock` 驱动，验证开盘立即刷新、午休/收盘停止和最后快照保留
- [ ] 5.8 扩展行情配置与状态 API，管理员按市场配置刷新频率，普通用户只选择已启用来源

## 6. 多市场持仓 API

- [x] 6.1 为添加/编辑持仓增加市场和建仓日期的接口失败测试，覆盖市场不匹配、未来日期和基金单位校验
- [x] 6.2 实现按证券目录校验的持仓创建/更新，禁止信任客户端提交的名称、交易所或币种
- [x] 6.3 扩展持仓查询为按市场过滤，保持用户隔离、排序和原有 A 股编辑/删除行为
- [x] 6.4 扩展操作审计记录市场、证券和建仓日期变化，但继续禁止保存数量、成本、密码或请求正文
- [x] 6.5 扩展管理员按用户持仓 DTO 和测试，证明跨市场响应仍不包含数量、成本、总市值和单只收益金额

## 7. 日收益与持有收益

- [x] 7.1 为股票开盘数量/开盘价基线、午休复用、缺失今开和幂等采集编写失败测试
- [x] 7.2 实现各股票市场开盘后的 `position_daily_baselines` 建立和来源/状态记录
- [x] 7.3 实现盘中新增或编辑持仓当日日收益不可用、下一交易日恢复的规则和测试
- [x] 7.4 实现股票日收益、日收益率、持有收益和持有收益率，覆盖零/缺失成本及部分行情
- [x] 7.5 实现基金相邻净值日收益和持有收益，覆盖净值缺失、建仓日期不早于净值日期和分批披露
- [x] 7.6 新增按市场/币种分组的 `GET /api/v1/me/returns`，禁止跨 CNY/HKD/USD 汇总
- [x] 7.7 停止计算年度/年化指标，新 DTO 删除相关字段，旧 performance 端点以 `null` 弃用字段兼容 0.1.5

## 8. 桌面端多市场交互

- [x] 8.1 为添加持仓的市场选择、市场隔离搜索、建仓日期和基金单位文案编写组件测试
- [x] 8.2 实现添加/编辑持仓表单的市场、数量、成本和日期交互，成功后跳转到对应市场子菜单
- [x] 8.3 实现始终可见的 A 股、港股、美股、公募基金四个持仓子菜单，以及按市场独立的空状态/交易状态
- [x] 8.4 重构持仓行展示，按 CNY/HKD/USD 或基金净值显示价格、日期、日收益和持有收益
- [x] 8.5 删除首页年度/年化卡片，改为按市场/币种展示日收益与持有收益且不跨币种合计
- [x] 8.6 实现七张指数概览卡的固定顺序、默认前三张首屏、横向滑动/触控板/触摸/按钮/键盘交互，以及单卡不可用/最后成功状态
- [x] 8.7 将行情联通灯改为右上角“联通检测”折叠面板，补充异常徽标、Escape、焦点恢复和中文来源名称
- [x] 8.8 在 1180×760、980×640、390×844、375×667 视口验证四类持仓、指数横向滑动、折叠面板和弹窗无不可达内容

## 9. 运维、可观测性与性能

- [ ] 9.1 为日历覆盖不足、目录过期、市场快照失败、基金净值日期落后和代码映射失败增加脱敏指标/日志
- [ ] 9.2 扩展 `/health` 与行情状态，区分组件存活、市场来源联通、日历覆盖和数据新鲜度
- [ ] 9.3 对港股/美股大快照做内存、Redis 大小、网络耗时和并发压测，设置有证据的超时/批量/刷新下限
- [ ] 9.4 更新生产环境示例、systemd 环境变量、部署手册、README 0.1.6 功能与数据延迟/合规说明

## 10. 验证、评审与发布

- [x] 10.1 执行 Java、Python、前端、Tauri、Flyway、Redis 集成测试和生产构建，保存真实输出
- [ ] 10.2 使用本地 PostgreSQL/Redis 跑完整 V5→V6 迁移和 A/H/美/基金端到端场景，验证旧 A 股客户端兼容
- [ ] 10.3 用真实 AKShare 非生产验证每类接口、列契约、行情时间、延迟标识和失败回退，不使用 Demo 数据
- [ ] 10.4 独立执行 verification pass，逐条核对六份 specs 并形成验证报告
- [ ] 10.5 独立执行 code-review pass，重点检查时区、币种、隐私、SQL 参数化、调度并发和缓存隔离
- [ ] 10.6 在 macOS 与 Windows 真机验证安装、登录、四类持仓、透明小窗、老板键、后台能耗和移动浏览器
- [ ] 10.7 生产发布前备份 PostgreSQL并完成恢复演练，部署兼容后端后观察 A/H/美各一轮开收盘和次日 07:00 基金任务
- [x] 10.8 经用户验收后更新到 0.1.6，提交推送、部署云服务器并构建/发布双平台安装包
