## 1. 发布工作流

- [x] 1.1 为桌面安装包 workflow 增加 `v*` 标签触发与标签场景下的生产 API 默认值
- [x] 1.2 增加依赖双平台构建结果的 Release job，并校验标签与应用版本一致
- [x] 1.3 汇总并使用唯一资产名称上传 DMG、APP ZIP、Windows NSIS EXE 与 SHA-256 文件

## 2. 自动验证

- [x] 2.1 扩展桌面安装包守卫测试，覆盖标签触发、最小权限、版本校验和 Release 资产发布
- [x] 2.2 运行桌面单元测试、前端生产构建和安装包工作流守卫测试

## 3. 正式发布

- [ ] 3.1 提交并推送修复后的发布流程到 `main`，创建并推送与应用版本一致的 `v0.1.1` 标签
- [ ] 3.2 监控 macOS、Windows 与 Release 任务成功，并核验 GitHub Release 资产和哈希文件
