# 代码复核

复核与 verification 分开执行。

## 结论

未发现阻断部署的问题。

## 核查项

- 全市场调度白名单只有 A 股、港股新浪，美股不存在全市场调用路径。
- 美股端点只接受规范的 NASDAQ/NYSE/AMEX 标识；传输内容不包含用户、持仓数量或成本。
- 美股批量按安全大小拆分；Java 侧去重并设置单轮上限。
- 上游异常、空结果或 Redis 写入异常均不会清空最后成功数据；刷新锁在 `finally` 中释放。
- Redis 更新脚本执行 `persist`，没有行情 TTL；旧东财配置只归一化，不执行破坏性删除。
- 页面不再展示或允许选择东财全市场来源，旧 localStorage 偏好写回新浪。
- 日志仅包含市场、来源、数量和异常类型，没有证券列表、用户信息或生产密钥。
- `git diff --check` 无空白错误；全仓搜索未发现美股全市场调度或旧美股快照状态标识。

## 后续观察

- 下一个三市场交易时段观察调度日志、Redis 元数据与 TTL。
- 若全体用户美股去重证券超过 2000，应按轮次持久化游标；当前规模下单轮上限足够。
