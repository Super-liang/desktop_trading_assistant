## 1. 行情快照可靠性

- [x] 1.1 为 Redis 快照仓储补充永久缓存、成功增量更新、空响应保留和元数据一致性测试
- [x] 1.2 移除快照及元数据 TTL，并实现仅更新成功返回证券的 Redis 写入逻辑
- [x] 1.3 补充交易时段边界、非交易时段不刷新及查询最后快照的测试与实现
- [x] 1.4 实现批量行情部分失败隔离与首页可区分的 API/上游/最后快照状态

## 2. A 股证券目录

- [x] 2.1 新增 PostgreSQL 证券目录 Flyway 迁移、实体、仓储及代码/名称查询测试
- [x] 2.2 在 AKShare 网关新增 A 股代码名称目录接口及响应校验测试
- [x] 2.3 实现每日北京时间 08:00 同步、首次空目录异步同步、分布式锁和失败保留
- [x] 2.4 新增受鉴权的证券目录搜索接口，并使搜索与实时行情模式解耦

## 3. 自选与持仓解耦

- [x] 3.1 修改持仓新增校验，只依赖 PostgreSQL 证券目录且事务内不查询实时行情
- [x] 3.2 修改持仓列表聚合，使单只证券无行情不影响其他自选项返回
- [x] 3.3 增加行情不可用时仍可新增自选、用户隔离和部分行情降级的后端测试

## 4. 桌面端交互

- [x] 4.1 将持仓数量和成本改为字符串草稿，支持清空、小数中间态及无持仓自选校验
- [x] 4.2 为证券搜索增加空查询保护、防抖、请求取消/竞态隔离、加载与错误状态
- [x] 4.3 为保存增加客户端超时、提交禁用和成功但无行情的恢复提示
- [x] 4.4 调整首页状态提示，区分 API 连接失败、行情源故障与最后快照降级，并为每只股票显示真实行情最后刷新时间
- [x] 4.5 补充数值输入、搜索竞态、超时、防重复提交和首页状态前端测试

## 5. 验证与交付

- [x] 5.1 运行 AKShare 网关、Spring API、桌面端单元与集成测试并记录结果
- [x] 5.2 独立执行 verification pass，验证 Redis 无 TTL、08:00 同步、非交易时段与端到端加自选
- [x] 5.3 独立执行 code-review pass 并修复高优先级问题
- [ ] 5.4 发布云端服务、执行迁移和健康检查，确认真实行情与最后快照行为
- [ ] 5.5 构建并发布更新后的 macOS 与 Windows 桌面安装包
