## 1. macOS 原生透明

- [x] 1.1 在 Tauri 配置中启用 macOS 私有 API 并调整 ticker 最小尺寸
- [x] 1.2 验证 ticker 全链路透明样式且主窗口不受影响
- [x] 1.3 如官方透明配置不足，增加仅 macOS 生效的原生 clear-color 兜底

## 2. 响应式缩放与重排

- [x] 2.1 实现可测试的 `0.7–1.15` 宽高缩放计算
- [x] 2.2 使用 ResizeObserver 将实时缩放值和布局模式应用到 ticker
- [x] 2.3 为紧凑行情表增加五列、三列多行和两列多行响应式布局
- [x] 2.4 确认所有尺寸保留核心字段、透明度控制、拖动、隐藏和老板键

## 3. 验证

- [x] 3.1 增加缩放边界、尺寸更新和响应式渲染测试
- [x] 3.2 执行完整前端测试、生产构建、Rust 检查与 OpenSpec 严格校验
- [x] 3.3 构建 macOS `.app`，验证真实背景穿透和默认/中等/最小三种尺寸
