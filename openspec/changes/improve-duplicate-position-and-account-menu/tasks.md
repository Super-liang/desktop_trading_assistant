## 1. 后端重复持仓契约

- [x] 1.1 先编写测试，覆盖重复创建返回结构化 409、不同用户隔离和同用户创建串行化
- [x] 1.2 增加按用户和规范证券键查询、悲观写锁查询及重复持仓业务异常
- [x] 1.3 将创建接口重复场景转换为 `POSITION_ALREADY_EXISTS`，禁止再返回 500

## 2. 后端原子累加

- [x] 2.1 先编写测试，覆盖加权平均成本、零持仓首次追加、非法数量/成本和越权 ID
- [x] 2.2 新增持仓累加端点，在事务内锁定当前用户持仓并更新数量与加权成本
- [x] 2.3 保留建仓日期等不可变字段并写不含数量/成本的持仓更新审计

## 3. 前端重复确认流程

- [x] 3.1 扩展 API 错误类型与累加请求方法，保留 HTTP 状态、错误码和已有持仓 ID
- [x] 3.2 先编写添加弹窗测试，覆盖确认累加、取消不写、普通错误和超时保护
- [x] 3.3 实现“已存在，是否累加持仓”确认与成功后原市场跳转/刷新

## 4. “我的”账户中心

- [x] 4.1 先编写桌面和手机导航测试，覆盖普通用户/管理员入口及账户操作可达性
- [x] 4.2 新增“我的”页面，复用修改密码、退出当前设备、退出全部设备和注销账号流程
- [x] 4.3 清理侧边栏和移动“更多”中的重复账户操作，管理员用户管理保持可达
- [x] 4.4 优化桌面与手机响应式布局，危险操作独立分区且不产生不可达内容

## 5. 验证与交付

- [x] 5.1 运行 Java 定向与全量测试、前端定向与全量测试及 Web 生产构建
- [x] 5.2 在本地可见 Terminal 环境验证重复添加、确认/取消、我的页面和四项账户入口
- [x] 5.3 独立执行 verification 并记录并发与未覆盖场景
- [x] 5.4 独立执行 code review，修复阻断级问题后复验
- [ ] 5.5 经用户确认后再提交、部署或发布客户端
